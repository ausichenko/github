<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/errorImage"
            android:layout_width="@dimen/layout_error_image_size"
            android:layout_height="@dimen/layout_error_image_size"
            android:layout_marginBottom="@dimen/layout_error_image_margin_bottom"
            tools:src="@drawable/ic_error" />

        <TextView
            android:id="@+id/errorMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/secondary_text"
            android:textSize="@dimen/layout_error_message_text_size"
            android:textStyle="bold"
            tools:text="Some error message" />

    </LinearLayout>

</layout>